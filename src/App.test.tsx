export {}

// import { render, screen } from '@testing-library/react';
// import App from './App';

// test('renders ASCA Protocol text', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/ASCA Protocol/i);
//   expect(linkElement).toBeInTheDocument();
// });
